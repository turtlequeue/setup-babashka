name: Main dev test

on: [push]

jobs:
  simple:
    # runs-on: ubuntu-latest
    runs-on: windows-latest
    steps:
      - name: Setup Babashka
        uses: turtlequeue/setup-babashka@feature/windows
        with:
          babashka-version: 0.2.3

      - name: Check bb runs
        run: bb --version
